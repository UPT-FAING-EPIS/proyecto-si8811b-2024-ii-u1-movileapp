namespace AsistenciaApp;

public partial class MainPage : ContentPage
{

	public MainPage()
	{
		InitializeComponent();
	}

    private async void logginBTN_Clicked(object sender, EventArgs e)
    {
        //if (string.IsNullOrWhiteSpace(usernameEntry.Text) || string.IsNullOrWhiteSpace(passwordEntry.Text))
        //{
        //    await DisplayAlert("Campos vacíos", "Por favor, ingrese su usuario y contraseña.", "OK");
        //    return;
        //}

        
        //const string validUsername = "Rodrigo";
        //const string validPassword = "1510";

        
        //string username = usernameEntry.Text.Trim(); 
        //string password = passwordEntry.Text.Trim();

        
        //if (username == validUsername && password == validPassword)
        //{
        //    await DisplayAlert("Inicio de sesión", "¡Inicio de sesión exitoso!", "OK");
        //    await Navigation.PushAsync(new Asistencia());
        //}
        //else
        //{
        //    await DisplayAlert("Inicio de sesión fallido", "Usuario o contraseña incorrectos.", "OK");
        //}

    }
}

